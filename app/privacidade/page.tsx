"use client";
/* eslint-disable @next/next/no-html-link-for-pages -- navegação pública preserva o padrão visual. */
import {useEffect,useState} from "react";export default function PrivacyPage(){const [policy,setPolicy]=useState("Seus dados são utilizados apenas para atendimento, agendamento e comunicação autorizada com o studio.");useEffect(()=>{fetch("/api/settings").then(r=>r.json()).then(p=>setPolicy(p.settings?.privacyPolicy??policy)).catch(()=>undefined);},[policy]);return <main className="legal-page"><a href="/">← Voltar ao início</a><p className="eyebrow">Transparência e cuidado</p><h1>Política de <em>privacidade.</em></h1><div>{policy}</div><p>Para solicitar correção, exportação, anonimização ou exclusão dos seus dados, entre em contato diretamente com o studio.</p></main>}
