import { eq } from "drizzle-orm";
import { getDb } from "../../../db";
import { salonSettings } from "../../../db/schema";
const defaults={salonName:"Jaqueline Justino Beauty Studio",address:"",phone:"",whatsapp:"",instagram:"",cancellationHours:24,depositPercent:0,toleranceMinutes:15,rescheduleAllowed:true,noShowBlockThreshold:3,noShowBlockDays:30,cancellationPolicy:"Cancelamentos e reagendamentos devem ser solicitados com pelo menos 24 horas de antecedência.",privacyPolicy:"Seus dados são utilizados apenas para atendimento, agendamento e comunicação autorizada com o studio.",homepageHeadline:null,homepageDescription:null,bannerImageUrl:null,primaryColor:"#0B0B0B",accentColor:"#D4AF37"};
export async function GET(){try{const db=await getDb();const [row]=await db.select().from(salonSettings).where(eq(salonSettings.id,"studio")).limit(1);return Response.json({settings:row??defaults},{headers:{"Cache-Control":"no-store"}});}catch{return Response.json({settings:defaults},{headers:{"Cache-Control":"no-store"}});}}
